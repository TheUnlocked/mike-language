// import { DiagnosticCodes } from '../../src/diagnostics/DiagnosticCodes'

on test() {
    //v expect diagnostics ~ has ~ id == DiagnosticCodes.CannotInferSequenceLiteralType
    [1];
    // v expect diagnostics ~ has ~ id == DiagnosticCodes.CannotInferSequenceLiteralType
    Stack[];
}

// assert diagnostics.length == 2