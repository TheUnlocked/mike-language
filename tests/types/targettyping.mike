param foo: (Queue<boolean>) => unit;

on _() {
    //                                v expect $t == type('Stack<Map<int, string>>')
    //                                 v expect $t == type('Map<int, string>')
    let x1: Stack<Map<int, string>> = [{}];

    // v expect $t == type('unit')
    //  v expect $t == type('Queue<boolean>')
    foo([]);
}

// assert diagnostics ~ none